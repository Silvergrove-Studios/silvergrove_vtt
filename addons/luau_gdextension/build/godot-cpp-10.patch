diff --git a/CMakeLists.txt b/CMakeLists.txt
index 5945cea..ebaa722 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -32,8 +32,9 @@ FetchContent_Declare(
 FetchContent_Declare(
     GodotCpp
     GIT_REPOSITORY https://github.com/godotengine/godot-cpp.git
-    GIT_TAG godot-4.5-stable
+    GIT_TAG 10.0.0-stable
 )
+set(GODOTCPP_API_VERSION "4.7" CACHE STRING "" FORCE)
 FetchContent_MakeAvailable(Luau GodotCpp)
 
 # Import GodotCPP module for documentation generation
diff --git a/src/lua_state.cpp b/src/lua_state.cpp
index 90e30ab..987b8f0 100644
--- a/src/lua_state.cpp
+++ b/src/lua_state.cpp
@@ -1,3 +1,4 @@
+#include <godot_cpp/classes/ref.hpp>
 #include "lua_state.h"
 
 #include "bridging/array.h"
@@ -422,7 +423,7 @@ void LuaState::setup_vm()
 Ref<LuaState> LuaState::bind_thread(lua_State *p_thread_L)
 {
     Ref<LuaState> state;
-    state.reference_ptr(memnew(LuaState(p_thread_L, get_main_thread())));
+    state = memnew(LuaState(p_thread_L, get_main_thread()));
     return state;
 }
 
@@ -1476,7 +1477,7 @@ Ref<LuaDebug> LuaState::get_info(int p_level, const String &p_what)
     ERR_FAIL_COND_V_MSG(!is_valid(), Ref<LuaDebug>(), "Lua state is invalid. Cannot get debug info.");
 
     Ref<LuaDebug> debug_info;
-    debug_info.reference_ptr(memnew(LuaDebug));
+    debug_info = memnew(LuaDebug);
     if (lua_getinfo(L, p_level, p_what.utf8().get_data(), debug_info->ptrw()))
     {
         return debug_info;
@@ -2114,12 +2115,12 @@ Ref<LuaState> LuaState::find_or_create_lua_state(lua_State *p_L)
         lua_State *main_thread_L = lua_mainthread(p_L);
         if (p_L == main_thread_L)
         {
-            state.reference_ptr(memnew(LuaState(p_L)));
+            state = memnew(LuaState(p_L));
         }
         else
         {
             Ref<LuaState> main_thread_state = LuaState::find_or_create_lua_state(main_thread_L);
-            state.reference_ptr(memnew(LuaState(p_L, main_thread_state)));
+            state = memnew(LuaState(p_L, main_thread_state));
         }
     }
 
diff --git a/src/lua_state.h b/src/lua_state.h
index 6c1c262..8245a33 100644
--- a/src/lua_state.h
+++ b/src/lua_state.h
@@ -1,6 +1,7 @@
 #pragma once
 
 #include <godot_cpp/classes/ref_counted.hpp>
+#include <godot_cpp/classes/ref.hpp>
 #include <godot_cpp/core/binder_common.hpp>
 #include <lua.h>
 
diff --git a/src/luau_script.cpp b/src/luau_script.cpp
index a696b3b..d047053 100644
--- a/src/luau_script.cpp
+++ b/src/luau_script.cpp
@@ -1,3 +1,4 @@
+#include <godot_cpp/classes/ref.hpp>
 #include "luau_script.h"
 
 #include "luau.h"
diff --git a/src/luau_script.h b/src/luau_script.h
index d0d01c6..0b667db 100644
--- a/src/luau_script.h
+++ b/src/luau_script.h
@@ -1,3 +1,4 @@
+#include <godot_cpp/classes/ref.hpp>
 #pragma once
 
 #include "lua_compileoptions.h"
diff --git a/src/register_types.cpp b/src/register_types.cpp
index bec8609..b0cc1d6 100644
--- a/src/register_types.cpp
+++ b/src/register_types.cpp
@@ -1,3 +1,4 @@
+#include <godot_cpp/classes/ref.hpp>
 #include "register_types.h"
 
 #include "lua_compileoptions.h"
